$(document).ready(function(){
    $('.burgermenu').on('click', function(){
        // $('.mobile-nav').toggle(slow);
        // $('.mobile-nav').toggle(fast);
        // $('.mobile-nav').toggle(3000);
        // $('.mobile-nav').fadeToggle();
        // $('.mobile-nav').slideToggle();
        $('.mobile-nav').toggle();
    })
});